// this file tests function calls
global int g;

int increment(int x)
{
	int f;
	f = x + 1;
	return f;
}

int main()
{
	int first;
	int second;

	first = 1;
	second = 2;
	first = increment(first);
	second = increment(second);
	printint(first);
	printint(second);

	return 0;
}